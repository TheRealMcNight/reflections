What happens when you initialize a repository? Why do you need to do it?

    It will list out the files you currently have. It sets up the repository to be pushed to in the future.

How can you use the staging area to make sure you have one commit per logical change?

    Not sure.

What are some situations when branches would be helpful in keeping your history organized? How would branches help?

    If you are working in a group and want to test some new additions and don't want to affect the master branch.

How do the diagrams help you visualize the branch structure?

    It helps you visualize the branch structure better since branchs and commits can be created in a non-linear fashion. It also helps you with reachability.

What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

    The coin change is now reachable in in the master. We represent it in the diagram to show how each change connects to one another.

What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

    The automatic takes away some human error. A con is that it can be tedious if there are conflicts that should be simple,  but not the same for git.